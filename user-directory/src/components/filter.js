import React from 'react';
import {users} from "../data";












// FUNCTION that allows a user to FILTER the table of USERS by a criteria

// DEFINE criteria (department?)
// React useEffect/?
// Button on click (hook)?